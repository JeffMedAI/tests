# Session Summary — 2026-06-07 ~13:00–13:35

## What we did

### 1. Watchdog investigation and full service recovery
- **Root cause found:** Scheduled tasks (JeffLocal-Watchdog, JeffLocal-HealthMonitor) were missing — not re-registered after 2026-06-03 watchdog PS5.1 patch
- **Watchdog was running** (continuous loop process alive) but had no crash-recovery backstop
- **n8n down since 19 May** — pattern: start → exit in 1 second. Root cause: ghost process holding port 5678 after previous crash. Fixed by Saeed manually starting n8n to claim the port cleanly; watchdog then picked it up
- **Sandbox dashboard (5000) down** — root cause: watchdog inline restart used `main:app` from inside `app/` directory, causing `ImportError: attempted relative import`. Fixed by creating `_launch_sandbox.ps1` matching production pattern (`app.main:app` from parent directory). Watchdog updated to use it.
- **Registry Run key: restored** (`install_scheduled_tasks.ps1`)
- **Scheduled tasks:** DailyPurge + DailyBackup updated OK; JeffLocal-Watchdog + JeffLocal-HealthMonitor required admin — Saeed imported via Task Scheduler GUI using `scripts/service_control/JeffLocal-Watchdog-task.xml`
- **Restart caps cleared** for n8n and SandboxDashboard after services recovered

### 2. n8n encryption key rotation
- No credentials stored in n8n — rotation simplified
- Old key: `L3cUkqVwNM3gSaVUwdb0E4/wYXSNJ7Ci` (retired, was visible in session)
- New key: `16fOETthAaiQ8LpIkWj7SgcmPJDbxsn9` (active)
- Key file must be written UTF-8 **without BOM** — PowerShell 5.1 `Set-Content -Encoding UTF8` adds BOM which breaks n8n. Use `[System.IO.File]::WriteAllText` with `New-Object System.Text.UTF8Encoding $false`

### 3. Deploy readiness assessment — sandbox → production
- 149/149 tests pass (after fixing stale `pytest-of-s5256` temp dir — `PermissionError: Access is denied`)
- pytest needs `--basetemp="C:\JeffLocal\pytest-tmp"` to avoid Windows AppData temp lock
- **6 core files differ** between sandbox and production (no WhatsApp files — Phase 2, excluded)
- Security Agent review completed — found one CRITICAL: both `set_cookie` calls in `main.py` missing `secure=True` (regression vs production). **Fixed** (lines 124 and 200 in sandbox/dashboard/app/main.py)
- Security Agent verdict: **APPROVE** — all conditions met
- **Deploy awaiting Saeed's explicit "approved"**

## Current service state (end of session)
- Production Dashboard (8765): UP
- n8n (5678): UP — new encryption key active
- Sandbox Dashboard (5000): UP — launch script fixed
- Ollama: UP
- Cloudflare Tunnel: UP
- Registry Run key: RESTORED
- JeffLocal-Watchdog-task: REGISTERED (via XML import)

## Files created/changed this session
- `scripts/service_control/_launch_sandbox.ps1` — NEW (sandbox launcher, `app.main:app` from parent dir)
- `scripts/service_control/watchdog.ps1` — updated sandbox restart block to use `_launch_sandbox.ps1`
- `scripts/service_control/JeffLocal-Watchdog-task.xml` — NEW (Task Scheduler import file)
- `sandbox/dashboard/app/main.py` — `secure=True` restored on both `set_cookie` calls (lines 124, 200)
- `C:\Users\s5256\.n8n\config` — new encryption key (UTF-8 no-BOM)
- Memory: `reference_service_control.md` — fully updated with launch script details, restart cap commands, known issues

## Pending deploy (needs Saeed approval)
Copy 6 files from sandbox to production (excluding whatsapp_*.py):
- `sandbox/dashboard/app/__init__.py` → `dashboard/app/`
- `sandbox/dashboard/app/audit.py` → `dashboard/app/`
- `sandbox/dashboard/app/auth.py` → `dashboard/app/`
- `sandbox/dashboard/app/importer.py` → `dashboard/app/`
- `sandbox/dashboard/app/models.py` → `dashboard/app/`
- `sandbox/dashboard/app/main.py` → `dashboard/app/`

Security Agent: APPROVED. Watchdog will auto-restart production after copy.

## Still open from PROJECT_MEMORY
- HMAC payload verification on n8n side: not yet complete
- GDPR purge scheduled task: needs admin PowerShell to register
- WhatsApp Meta credentials: awaiting Saeed
- NHS SBS Ariba registration: deadline 23 June 2026
- Staff accounts for pilot: awaiting Saeed
