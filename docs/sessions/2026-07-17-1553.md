# Session — 2026-07-17 (afternoon/evening)

## WHAT WE DID
Investigated stray 0-byte garbage files (root + dashboard/): '`, `12,+`, `44`, `500`, `HTTP`, `Response`, `cannot`, `copies`, `glob`, `int`, `not`, `redirect`, `see`, `the`, `dict[str`, plus dashboard/ copies. Root cause: unquoted `<`/`>` characters in pasted text/commands reaching git-bash, which treats them as real redirect operators and silently creates an empty file named after the next word. Recurring since 26 June, one per session roughly. No data risk, all empty. Saeed approved cleanup — all 19 removed.
Fixed the 3 required changes on feature/secrets-loader Security flagged last session: struck the false "gated by test_mode" claim (test_mode is attacker-controlled, gates nothing), downgraded the directory-ACL check from "trust boundary" to documented advisory layer, capped+sanitised the refused-key log line. Security: APPROVE. Saeed approved merge. Merged 06af07e.
Built multi-tenancy step 2 (governance/MULTI_TENANCY_PROPOSAL.md §8) on feature/multitenancy-db-path, in worktree C:\JeffLocal_multitenancy:
- `JEFFLOCAL_DB_PATH` env var in db.py, TDD'd (test written first, watched fail, then passed).
- `-Tenant` param on `_launch_dashboard.ps1`. No flag = today's exact behaviour (port 8765, same log, same DB) — verified by reading every real caller (restart_all.ps1, watchdog.ps1 both call with no args).
- New `_load_tenant_config.ps1`: allowlisted loader for JEFFLOCAL_DB_PATH/PORT/TENANT_NAME, tenant name regex-validated against path traversal, fail-loud on unknown/incomplete tenant (unlike secrets, missing tenant config is NOT a safe default).
- Tenant onboarding scaffolding: `config/tenants/_template.env.example` + `test-tenant-1.env` placeholder fixture (Saeed: use a placeholder, not real pilot-practice names, until we have them — St Marks Pharmacy confirmed as the first REAL tenant to onboard, separate from GP triage scope).
Security review round 1: APPROVE WITH CHANGES. Found a real bug — Set-Item failures are non-terminating in PowerShell, so a silently-failed env var set was still recorded as "loaded," which would have let a tenant fall through to db.py's DEFAULT database (cross-tenant patient data mixing — the exact failure separate databases exist to prevent). Fixed (try/catch + -ErrorAction Stop), plus 3 recommended hardenings (log sanitising, directory-ACL check matching the secrets loader, .gitignore rule). Security review round 2: APPROVE, no further changes.
Discovered while testing: production's real `C:\JeffLocal\config` IS writable by Authenticated Users (verified read-only, no changes made) — this is the pre-existing open item #3, not new. Consequence: the new tenant ACL check will correctly refuse every `-Tenant` launch (including the test placeholder) until that's fixed. Confirmed this is the check working as designed by locking down ACL on the isolated worktree only and re-running all scenarios successfully.
Saeed asked to "check EVERYTHING" before merge. Found and closed a real gap: the PowerShell changes had only been manually smoke-tested (ad-hoc, thrown away), not TDD'd — no PS test framework existed in this repo. Wrote a committed, repeatable regression suite (`scripts/service_control/tests/test_load_tenant_config.ps1`, 12 assertions) and mutation-tested it (deliberately broke the ACL check, confirmed the suite caught it, restored, confirmed green again) before committing, to prove it's a real test and not a tautology.
Merged feature/multitenancy-db-path → main (390f774), Saeed approved. Pushed to origin. Verified 391 Python tests + 12 PS assertions green on the production tree post-merge.

## WHAT TO DO NEXT
Multi-tenancy step 3: backup + migrate `dashboard.sqlite` → `churchtown.sqlite`. Needs Saeed on the day (production DB, though all data is fake). See governance/MULTI_TENANCY_PROPOSAL.md §8.
Before step 3 can do anything useful: fix the config directory ACL (open item #3) — otherwise no tenant, including a future real churchtown.env, will actually load.
Steps 4-8 of the multi-tenancy sequence are entirely unstarted: stand up St Marks instance, tenant picker page + Avamed admin accounts, per-tenant backup/purge + migration runner, set STMARKS_INTAKE_SECRET, go-live compliance sign-off. Realistic estimate: 2 of 8 steps done, this is the foundation not the building.
Two worktrees from this session are fully merged and safe to remove when convenient: C:\JeffLocal_secrets and C:\JeffLocal_multitenancy. Not deleted — left for Saeed/next session to confirm.
Lint (ruff/pyflakes) in test gate — still open, carried from 15/16 Jul.

## BLOCKERS
NOTHING IS LIVE. ALL PATIENT DATA IS FAKE. Below is pre-go-live debt, not active incidents.
The directory-ACL fix (item #3) is now blocking two things, not one: secrets loading (once secrets.env is actually created) AND all tenant onboarding (confirmed today). This raises its priority.

## PENDING SAEED
1. Directory ACL fix on C:\JeffLocal + C:\JeffLocal\config (Authenticated Users has write) — now confirmed blocking tenant onboarding, not just theoretical.
2. Multi-tenancy step 3 sign-off when ready: backup + migrate the production DB to churchtown.sqlite.
3. Real staff accounts (names, roles, emails) — still open.
4. Governance gates 1-7 — still open, cannot be delegated.
5. JEFF_WEBHOOK_SECRET — still unset, endpoint still open.
6. Real HMAC secret rotation (voice_agent_hmac_secret.txt, in git history) — still open.
7. Cleanup of the two now-merged worktrees (C:\JeffLocal_secrets, C:\JeffLocal_multitenancy) — low priority, safe whenever.
