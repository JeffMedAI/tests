# Session — 2026-07-17 (evening, continued)

## WHAT WE DID
Ran `/fewer-permission-prompts`. Scanned 27 recent chat logs. Added a short list of safe "read-only, no changes" commands to `.claude/settings.json` so Claude stops asking permission for them: schtasks query (both slash styles seen from git-bash), browser read_page/get_page_text/find/read_network_requests, one screenshot tool. Skipped anything that writes, deletes, or runs code — those still ask every time, on purpose.
Wrote (did NOT run) `scripts/service_control/fix_directory_acl.ps1` — exact fix commands for open item #3 (C:\JeffLocal + config folder writable by too many Windows accounts). Confirmed the problem is real by checking the real folder permissions (read-only check). Changing Windows security settings is outside what Claude runs directly — Saeed or an admin runs this one manually. REMINDER SET: Saeed asked to be reminded to run this script later.
Multi-tenancy step 3 done: built `scripts/tenant/migrate_to_tenant_db.py` in a worktree, tests written first (TDD), copies dashboard.sqlite -> churchtown.sqlite safely while the dashboard stays live (same trick as the existing backup script). Security Agent review caught a real bug before merge: no check stopping someone from accidentally pointing the copy at itself, which could have deleted the live database. Fixed, merged to main, pushed.
Ran it for real, with Saeed's go-ahead given this session: backed up dashboard.sqlite, then made churchtown.sqlite. Checked both match exactly — 78 cases, 5 staff logins, 1,251 audit records, both files pass their internal health check.
Found one more bug running against the real data: my table names were wrong (guessed common names, real ones were different). The copy itself was fine — only the "did it work" check crashed. Fixed the names, checked again directly against the real files — good match, confirmed.
Checked the live dashboard afterward: still working, still shows 78 cases, nothing broken. We did NOT switch the live site over to the new database — that's a later step.
Updated CLAUDE.md per Saeed's instruction: added "never guess or lie" and "confidence tags" wording directly into rules 4 and 5 (both already existed — reaffirmed, not duplicated). Added explicit instruction that /caveman and /superpowers stay on for every response, all session, not just the start. Added "explain like a fourth grader" language throughout.

## WHAT TO DO NEXT
Saeed to run `scripts/service_control/fix_directory_acl.ps1` (as admin) — REMINDER, carried forward every session until done.
Step 4: stand up the St Marks tenant instance + hostname + staff accounts — still blocked by the same folder-permission issue above (config folder must be fixed first or no tenant, including St Marks, can actually start).
Steps 5-8 of the multi-tenancy plan are still untouched: tenant picker page, per-tenant backup/purge, STMARKS_INTAKE_SECRET, go-live sign-off.

## BLOCKERS
NOTHING IS LIVE. ALL PATIENT DATA IS FAKE. This is pre-go-live homework, not a live problem.
Folder-permission issue (item #3) now blocks step 4 the same way it blocked step 3 setup — fix this before starting step 4.

## PENDING SAEED
1. Run `scripts/service_control/fix_directory_acl.ps1` (admin PowerShell) — reminder, not yet done.
2. Real staff accounts (names, roles, emails) — still open.
3. Governance gates 1-7 — still open, cannot be delegated.
4. JEFF_WEBHOOK_SECRET — still unset, endpoint still open.
5. Real HMAC secret rotation (voice_agent_hmac_secret.txt, in git history) — still open.
6. Cleanup of two old merged worktrees (C:\JeffLocal_secrets, C:\JeffLocal_multitenancy) — low priority, safe whenever.
7. Say go when ready to start step 4 (blocked on item 1 above first).
