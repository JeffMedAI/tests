# SESSION SUMMARY - 2026-07-21 (work spanned 2026-07-20 evening into 21st)
# Tool: Claude Code (interactive session with Saeed)

---

## WHAT WE DID

- Session start: first read stale PROJECT_MEMORY, Saeed corrected. Re-read - found this morning's
  (07-20) session already done: daily-brief crash fixed, directory ACL fix run by Saeed. That
  unblocked multi-tenancy step 4.
- Planned step 4 in plan mode. 3 Explore agents mapped: config scaffolding, ops plumbing, staff
  bootstrap. 1 Plan agent designed the build. Plan approved by Saeed.
- Saeed decisions locked: generic placeholder identity ("Tenant 2", not "St Marks") until go-live;
  seed BOTH placeholder admin AND staff logins per tenant; localhost:8766 only, no Cloudflare yet.
- Built in isolated worktree C:\JeffLocal_tenant2 (branch feature/multitenancy-tenant2). Never
  touched C:\JeffLocal's own branch for WIP.
- Found + fixed real bug: db.py init_db() seeded 3 demo staff rows (incl "Admin Demo" with
  password_hash=NULL, unusable) on ANY empty DB. Now gated to default/no-tenant instance only.
  Would have hit every future tenant.
- New scripts/tenant/create_tenant_db.py: fresh-DB counterpart to migrate_to_tenant_db.py. Seeds
  2 placeholder logins (admin+staff), forced password change, audit-logged, reuses auth.py hashing.
- New config/tenants/tenant2.env + governance/TENANT_REGISTRY.md.
- backup_db.py: loops all tenant DBs, skips not-yet-provisioned ones (not a failure).
- watchdog.ps1: new Tenant2Dashboard entry (8766). ProductionDashboard block diffed byte-identical.
- register_scheduled_tasks.ps1: second GDPR-purge task for tenant2.
- TDD throughout. Security Agent reviewed auth-touching changes: APPROVE. 2 optional notes applied
  (pin_hash=NULL not fake PIN; audit events on script seeding).
- Full live E2E: both placeholder accounts log in, forced through change-password flow, tenant2
  case list + audit log isolated from churchtown, churchtown (case_count 78) unaffected throughout.
- 436 Python tests green (393 dashboard post-merge + 54 scripts) + 2 PS regression tests green.
- Merged to main (85815a1), pushed. Hit stale .git/index.lock (recurring repo issue) - removed it,
  merge succeeded. Worktree + branch cleaned up.
- Created real tenant2.sqlite in production data path (2 placeholder logins, case_count 0, integrity ok).
- Logged step 4 in CHANGELOG.md.
- Wrote scripts/service_control/apply_tenant2_ops.ps1 - one-time ADMIN script to register the new
  scheduled task + restart the watchdog. First write had em-dash mojibake (PS 5.1 encoding) - rewrote
  ASCII-only, parses clean. Committed + pushed.

## WHAT TO DO NEXT

- Saeed runs apply_tenant2_ops.ps1 in ADMIN PowerShell. Two things a non-elevated session cannot do:
  (1) register the tenant2 GDPR-purge scheduled task, (2) restart the watchdog so it loads the new
  Tenant2Dashboard entry. Until then tenant2 works but is not watchdog-managed.
- After Saeed runs it: confirm tenant2 (8766) auto-managed, churchtown (8765) still 78.
- Then step 5 (governance/MULTI_TENANCY_PROPOSAL.md §8): tenant picker page + Avamed admin account
  per tenant. This is where the tenant-admin / avamed-super-admin role rename belongs (NOT step 4).
- Replace placeholder logins (tenant2 + churchtown's existing 5) with real names/emails before any
  go-live.

## BLOCKERS

- apply_tenant2_ops.ps1 needs elevation - Saeed to run (same category as the ACL fix). Not a code
  blocker; tenant2 is fully built and works, just not yet watchdog-managed.

## PENDING SAEED

1. Run apply_tenant2_ops.ps1 (admin PowerShell) - registers tenant2 purge task + restarts watchdog.
2. Cloudflare hostname for tenant2 + churchtown rename (dashboard.app-avamed.uk -> churchtown.app-avamed.uk)
   - deliberately deferred, Saeed wants guidance when ready.
3. Standing security items unchanged: unauth intake endpoint, HMAC secret rotation, real staff
   accounts, governance gates 1-7.
