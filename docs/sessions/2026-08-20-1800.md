# SESSION SUMMARY - [2026-08-20 18:00]
# Tool: Claude Code
# Written by: Claude at session end (replaces the automated placeholder written earlier today)

---

## WHAT WE DID

- Fixed the daily brief. Three faults, all the same shape: looked fine, wasn't.
- Fault 1: briefs went stale 11-19 Aug, unnoticed for 8 days. Fallback to the newest log said so only in a small mid-message note.
- Fix: loud banner at the TOP of the brief, PER PROJECT. JeffLocal was dark 8 days while St Marks shipped 5 commits - the combined format hid the dead half behind the live half.
- Fault 2: auto-placeholder session logs would have defeated the new alarm (a file exists every day = no gap visible). Placeholders now carry an AUTOGEN-PLACEHOLDER marker and do not reset the staleness clock.
- Fault 3: "WhatsApp send failed" was a FALSE alarm. pywhatkit writes its ledger by relative path into C:\Windows\System32 under the scheduled task, denied AFTER delivery. Pinned CWD to C:\JeffLocal\logs\whatsapp.
- Turned the git safety net back ON. combined_brief called strategy_daily with -DryRun, which killed the memory write, commit, push and tag together. Added a narrower -NoSend switch.
- Found by test: `git ... 2>&1` under EAP=Stop promoted ordinary git NOTICES ("LF will be replaced by CRLF") to fatal errors, abandoning the commit and logging "Git push failed". Now judged on $LASTEXITCODE.
- Evening close now writes a REAL session log built from the day's git activity, not a stub. Records the actual branch instead of hardcoded "sandbox".
- Evening close now also refreshes HANDOFF.md when no hand-written close happened. Leaves a hand-written one alone.
- Cowork "Daily session end 1800" RETIRED. It cannot work - proven by experiment, not inference.
- Cowork writes each task's file into the folder the task points at (C:\JeffLocal\Scheduled), marks that a protected root, then drops any folder overlapping it. The task got no access to C:\JeffLocal at all.
- Log evidence: "[Lifecycle] Dropping folder overlapping protected root ... C:\JeffLocal (root: C:\JeffLocal\Scheduled)".
- Created a throwaway test task: Cowork made a new folder inside the project again. Structural, not a settings problem.
- Added a SessionStart hook (user level, ~/.claude/hooks/session_start_memory.py). Surfaces PROJECT_MEMORY + HANDOFF + newest session log at every session start, both projects. Warns on staleness and on placeholder logs.
- Live evening run: report, session log, commit, PUSHED the 16-commit backlog, restore tag cut (first in 3 weeks), WhatsApp delivered clean. Zero errors.

### Later the same day (16:09-16:39) - Saeed's follow-up instructions

- Close now counts UNCOMMITTED work, not just commits. A full day of work had read as "no work today" purely because nothing was committed yet.
- Close now commits EVERYTHING (`git add -A`), new files included, per Saeed. Safe because add -A honours .gitignore, which already excludes .env, *.sqlite, *.db, logs/, *.log, *.jsonl, queue/, outputs/, data/. All 324 untracked files checked first: no secrets, no patient data.
- Bug caught by test: the close edits PROJECT_MEMORY.md itself, so its OWN edit counted as work - the placeholder branch could never run and the staleness alarm would have been permanently dead. Own bookkeeping now excluded.
- 15 zero-byte shell-accident files ignored by name ("None", "Run", "dict[str", ...). Bracket patterns must be escaped or git treats them as globs and silently fails.
- **St Marks (SMCPHARMA) now has its own automated session close** - combined_brief.ps1 section 6b, same script with -ProjectName and its own paths.
- Swept the 324-file untracked backlog in a SUPERVISED commit rather than letting it land unattended at 19:00. That immediately exposed a real problem:
- **`git add -A` picked up SMCPHARMA as an embedded git repo (gitlink, mode 160000).** Anyone cloning JeffLocal would get an empty SMCPHARMA folder, and the close would re-commit a moving pointer nightly. Removed with `git rm --cached` (files untouched) and SMCPHARMA/ added to .gitignore.
- Deleted the stale scheduled_tasks.lock (31 days) and the spare C:\JeffLocal-Scheduled copy. Archived the Cowork task instructions to docs/archive/ first so deleting the task loses nothing.
- 29 automated assertions pass across staleness detection, session-log generation, HANDOFF generation, switch behaviour and the commit-everything contract (including: a gitignored secret stays OUT, genuinely new work goes IN).

---

## BLOCKERS

- None for the brief pipeline. It is fixed and proven live.
- Cowork scheduled tasks remain unusable for any folder they are pointed at. Worth reporting to Anthropic. Not blocking - PowerShell replaced it.
- Carried forward, unchanged since 11 Aug: 3 security items (unauth intake endpoint, HMAC secret in git history, directory ACLs).

---

## PENDING SAEED APPROVALS

- [ ] Delete the Cowork "Daily session end 1800" task. Deleting also removes C:\JeffLocal\Scheduled and clears the protected-root block. Disabling alone does not.
- [ ] Tidy C:\JeffLocal-Scheduled (spare copy of the task instructions) once the task is deleted.
- [ ] Clear the stale C:\JeffLocal\.claude\scheduled_tasks.lock (31 days old, owner process gone). Not a cause of anything - just rubbish.
- [ ] Report the Cowork protected-root defect to Anthropic?
- [ ] Write session logs / HANDOFF for SMCPHARMA too? That repo is under a standing read-only instruction from Saeed.
- [ ] Real staff accounts - names, roles, emails - to unblock pilot go-live.
- [ ] Governance gates 1-7 sign-off.
- [ ] JEFF_WEBHOOK_SECRET before any live Jeff traffic.
- [ ] The 3 security items above.

---

## WHAT TO DO NEXT SESSION

1. Check the 07:00 brief. No staleness banner = the whole chain is healthy.
2. Confirm the 19:00 run wrote a real session log and refreshed HANDOFF.md by itself.
3. Delete the Cowork task once Saeed approves.
4. Resume the security items - none can move without Saeed.

---

## GIT STATE

Latest commit: 02d064f fix(brief): stop the daily brief failing silently; retire the Cowork session close
Branch: main
Restore tag: restore/2026-08-20-1800
Backlog: cleared - 0 unpushed (was 16)
