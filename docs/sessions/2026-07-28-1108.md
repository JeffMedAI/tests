# SESSION SUMMARY — 2026-07-28 11:08

# Multi-tenancy Step 5: tenant picker + avamed-super-admin role + churchtown->tenant1 repoint.
# Long session, spanned usage-limit resets. Ran via /loop, self-paced.

---

## WHAT WE DID

- Step 5 DONE, merged, cutover run by Saeed, verified live.
- Built in isolated worktree. Design note first. TDD.
- New `avamed-super-admin` role. `/tenants` picker page — links only, never merged data.
- `admin` role unchanged = tenant-admin (no rename, §6b already settled that).
- Super-admin only creatable via seed script, never web UI (escalation guard, tested).
- 2 Security reviews (code, then cutover tooling). Both APPROVE-WITH-CHANGES. All fixes done.
- 2 real bugs caught pre-merge: FK-corruption in role migration (legacy_alter_table fix), silent
  super-admin self-downgrade on staff edit.
- Per-tenant one-time passwords (Saeed's call, matches Security). Printed to admin console only.
- Merged SELECTIVE files only — branch had 199k lines of graphify/junk cruft, excluded it.
- Repoint: 8765 now serves tenant1.sqlite (churchtown 78 cases), not default dashboard.sqlite.
- Saeed ran `apply_step5_cutover.ps1` elevated (locked-dir write + watchdog restart = needs admin).
- Verified live: 8765 fresh relaunch on tenant1 (dashboard-tenant1.log fresh, dashboard.log stale),
  case_count 78; 8766 untouched (0, uptime unchanged); /tenants gated (302); avamed-saeed seeded in
  both tenant DBs; dashboard.sqlite + churchtown.sqlite both preserved for rollback.
- Tests: 440 dashboard + 42 scripts/tenant = 482 green, 0 fail (production tree).
- Recovered 2 stuck automated evening-brief commits (stale git locks, docs-only, harmless).

## WHAT TO DO NEXT

- Saeed can log into /tenants as avamed-saeed with his OTP to eyeball the picker (Claude won't enter
  passwords). Confirms per-tenant isolation visually — structurally already guaranteed.
- Cloudflare hostnames: tenant2 public hostname, churchtown->churchtown.app-avamed.uk. Deferred,
  Saeed wants guidance when ready.
- Replace placeholder logins (avamed-saeed, churchtown's, tenant2's) with real staff names+emails
  before any real go-live.
- Optional: registry.json status field — tenant1 still says "planned", now actually running. Cosmetic.

## BLOCKERS

- None for step 5 — done + verified.
- Standing pre-go-live debt (not blocking, all data fake): unauth intake endpoint
  (/api/n8n/test-intake-batch), HMAC secret rotation, broader C:\JeffLocal+config ACL fix
  (07-20 fix incomplete, AU write via inheritance), governance gates 1-7.

## PENDING SAEED

- Nothing blocking. Step 5 signed off + run this session.
- When ready: Cloudflare hostname guidance; real staff account details for go-live.
