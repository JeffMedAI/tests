# Session — 2026-07-16 (ran past midnight, closed 2026-07-17 ~10:00)

## SAEED'S CORRECTIONS AT CLOSE (2026-07-17) — I had 3 things wrong
Multi-tenancy is for GP PRACTICES under JeffLocal (Churchtown + 4 more). St Marks is just a TENANT — stand-alone pharmacy, no "tenant pharmacies" under it. Not a special case. My governance note treated it as a foreign body. Corrected.
Avamed super-admin = TENANT SWITCHER. Switch tenant, view each tenant's data individually, provide support. Not practice staff. That IS what I recommended — I misread "see across tenants" as a merged view and argued against a position Saeed never held. §6 now settled, not blocked.
ALL patients are FAKE. Nothing live until compliant, tested, partner-approved. I framed the open endpoint as fabricated tasks landing "in front of reception staff" — there are no reception staff and no real patients. Overstated. Corrected everywhere.

## WHAT WE DID
Merged feature/case-alerts-and-stmarks → main (da24bb2). Beep-on-new-case poll, alert modal CSS, St Marks intake endpoint. 382 tests green. Redeployed, health OK.
St Marks Worker: wrote forwardToJeffLocal() in SMCPHARMA/src/index.js. Tested via wrangler dev against isolated local dashboard on :8798. Case landed correctly. Proved email path unaffected when forward target unreachable (same 503, same ~5ms). Committed+pushed (f9209d9).
Fixed stale SMCPHARMA HANDOFF.md that my own commit swept in (476270f). Lesson: pre-staged files ride along even when you `git add` specific paths.
Governance note on St Marks data sharing — written, then REWRITTEN (ef6dbbf) after Saeed corrected me: Avamed is PROCESSOR for both JeffLocal and St Marks, not controller. Real issue is two controllers sharing one DB with no tenant separation.
Saeed decided: multi-tenancy required, separate DB per tenant, St Marks staff get own logins, wants Avamed super-admin. Proposal written (1bed6fd).
Privacy policy line drafted naming Avamed as processor. On branch draft/privacy-avamed-processor — NOT pushed, NOT live. Needs pharmacist.
Built secrets loader (config/secrets.env + _load_secrets.ps1). Security BLOCKED it — my key regex validated name SHAPE not IDENTITY, so PYTHONPATH passed → RCE inside dashboard process. Fixed with allowlist. Re-review: APPROVE WITH CHANGES. Branch feature/secrets-loader, NOT merged.
Saeed reported a beep. Investigated. Root cause: importer re-imported all handoffs every 60s (only failures moved out) AND upsert_case didn't preserve imported_at. 5 test files from 14 Jul looked brand-new every minute. Fixed both. Security BLOCKED twice, then APPROVED. Merged b53847a, deployed, verified imported_at now frozen across 70s.
CAUGHT A CASE-LOSS REGRESSION I INTRODUCED (ebd395f). Running the test suite in the production tree retired real pending handoffs — file gone from prod inbox, case only in temp DB → patient's case silently lost. Proven with a canary. Fixed conftest + test_render_pages. Canary now survives a full suite run.
Cleaned up 2 canary rows that leaked into the production DB via the live importer. Archived to backup/canary_rows_removed_20260717.json first. Back to 78 cases.

## ACCESS MODEL DECIDED (2026-07-17, proposal §6b/6c/6d)
Super-admin tenant dropdown = LINK to the tenant's hostname, NOT a data switch (a switch needs a process connected to every tenant DB — forbidden). Cookies host-only today; never add domain=.
Roles: Avamed super-admin, tenant-admin (practice manager, own staff — Saeed: yes), staff, readonly. Staff isolation structural (staff_users per DB). Locums = one account per practice.
Onboarding SCRIPTED not web-UI (Saeed's choice). Tenant registry, ZERO patient data. Script must also do Cloudflare hostname + watchdog + backup + purge + Avamed admin.
Reporting: per-tenant free; Avamed rollup AGGREGATES ONLY, never touches a tenant DB.
TIME-SENSITIVE: Churchtown manual-triage baseline must be captured BEFORE go-live for ROI. Saeed will try; else labelled industry benchmark, never passed off as measured. Staff-workload reporting = employee monitoring, into the contract.

## WHAT TO DO NEXT
Multi-tenancy — DECIDED + SETTLED, ready to build. Separate DB per tenant. §8 of governance/MULTI_TENANCY_PROPOSAL.md has the step sequence. Access model in §6b/6c/6d. Gates go-live for EVERY tenant.
Merge feature/secrets-loader — Security says strike the overclaims first (see HANDOFF). It carries the per-tenant config, so it's step 1 of multi-tenancy.
No tenant's intake goes on into a shared DB. Do not set STMARKS_INTAKE_SECRET; same rule for GP practice #2.
Lint (ruff/pyflakes) in test gate — still open, carried from 15 Jul. pyflakes caught 2 real bugs this session that tests missed.
Evening-brief script corrupted-tail bug — still open.

## BLOCKERS
NOTHING IS LIVE. ALL PATIENT DATA IS FAKE. Below is pre-go-live debt, not active incidents. All must close before either business goes live. No time pressure — build properly.
Needs Saeed, in priority order:
1. /api/n8n/test-intake-batch accepts UNAUTHENTICATED requests (JEFF_WEBHOOK_SECRET unset → HMAC skipped). Reachable via Cloudflare tunnel. Writes queue envelopes, spawns pipeline subprocess. Its own docstring says remove before production. test_mode is NOT a guard — it's read from the attacker's payload.
2. Real HMAC secret COMMITTED to git: config/security/keys/voice_agent_hmac_secret.txt, since ff699b5. Needs ROTATION, not just untracking — it's in history.
3. C:\JeffLocal AND C:\JeffLocal\config writable by Authenticated Users. Root cause of the RCE. Fixing only the flagged ACE leaves CodexSandboxUsers + an orphan SID + a generic-rights ACE.
Standing: no staff accounts, governance gates 1-7 unsigned, Avamed not registered.

## PENDING SAEED
The 3 blockers above.
St Marks privacy line — pharmacist/DPO review before it can go live.
(§6 super-admin SETTLED 2026-07-17 — tenant switcher. Do not re-raise.)
