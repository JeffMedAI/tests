# SESSION SUMMARY — 2026-07-27 18:00
# Tool: Cowork (automated session end)
# Written by: Claude scheduled task at 18:00

---

## WHAT WE DID TODAY

- Multi-tenancy step 5 landed on main (82b7ba2): avamed-super-admin role added — lets Avamed
  support staff look at one tenant's data at a time via a new /tenants picker page. Never a
  merged cross-tenant view. Only created via seed_super_admin.py, not assignable through the
  staff web UI (tested to block privilege escalation).
- Same commit: a schema fix — widened the role field on staff_users/staff_invitations so it
  accepts the new role on existing tenant databases. A real bug (foreign-key corruption) was
  found and fixed in this migration before it landed.
- Same commit: staff_edit no longer silently downgrades an avamed-super-admin account by
  accident. New scripts: seed_super_admin.py (creates the role, one-time password per tenant)
  and rename_tenant_slug.py (renames churchtown -> tenant1, repoints its database).
- Cutover tooling landed (d368529): watchdog.ps1 updated so port 8765 will launch on the
  tenant1 database once tenant1.env exists — safe to ship now because it falls back to
  today's normal behaviour until that file exists. New apply_step5_cutover.ps1 script
  (mirrors step 4's pattern) does the actual switch: copies churchtown.sqlite to
  tenant1.sqlite, seeds the super-admin accounts, restarts the watchdog. NOT YET RUN.
- Recovered a stuck commit from yesterday (c8c7eae) — 2026-07-26's automated session-end
  commit had died mid-write (a stale lock file), no actual work was lost.

[Certain] — read directly from today's git log and commit messages.

---

## PENDING SAEED APPROVALS (from PROJECT_MEMORY)

1. The 3 security items — unauthenticated intake endpoint (JEFF_WEBHOOK_SECRET unset),
   HMAC secret committed to git history (needs rotation, not just untracking), directory
   ACLs on C:\JeffLocal + config. Fix script ready: scripts\service_control\fix_directory_acl.ps1.
2. Multi-tenancy — PROJECT_MEMORY's narrative text still says "steps 1-3 of 8 done,
   step 4 done 2026-07-21, step 5 next." That is now out of date: step 5 code merged
   today, and its cutover script (apply_step5_cutover.ps1) needs Saeed to run it in an
   ADMIN PowerShell window before tenant1 (churchtown) actually switches over on 8765.
   [Certain from git log that step 5 code merged — NOT verified that it has been run or
   tested live; treat as pending, not complete, until Saeed confirms.]
3. Real staff accounts — names, roles, emails needed to unblock pilot go-live.
4. Governance gates 1-7 sign-off — cannot be delegated.
5. JEFF_WEBHOOK_SECRET — must be set before any live Jeff traffic.
6. n8n API key rotation — confirmed "later".
7. St Marks privacy-policy line — drafted on branch draft/privacy-avamed-processor in
   SMCPHARMA, not pushed. Needs pharmacist/DPO review.

---

## OPEN TASKS (from PROJECT_MEMORY)

1. Fix n8n.py:319 HMAC fail-open; remove /api/n8n/test-intake-batch — Security, needs
   Saeed sign-off (auth logic). Endpoint is OPEN right now.
2. Rotate voice_agent_hmac_secret.txt (in git history) — Security, Saeed to action.
   Rotation, not untracking.
3. Multi-tenancy: separate DB per tenant — step 5 code now merged (today), cutover
   script written but not run. Next real step is Saeed running apply_step5_cutover.ps1.

---

## WHAT TO DO TOMORROW

1. Confirm with Saeed whether apply_step5_cutover.ps1 has been run. If not, that is the
   single next action to unblock tenant1 naming + super-admin picker going live.
2. Chase the 3 outstanding security items (unauthenticated intake endpoint, HMAC secret
   rotation, directory ACLs) — all still need Saeed's explicit sign-off.
3. Update PROJECT_MEMORY's "CURRENT STATUS" narrative section to reflect step 5 as
   code-complete-but-not-yet-cut-over (this session only touched the required index
   fields, not the full narrative — flagging for next session to reconcile properly).

---

## GIT STATE

Latest commit: d3685293edee39134677ec9113a1a31968fcd469 — step5 cutover tooling: watchdog
repoint (8765 -> tenant1) + apply script
Branch: main (confirmed via git status; CLAUDE.md notes branch is sometimes called "sandbox"
by habit but the checked-out branch today is main)
