# SESSION SUMMARY — [2026-07-18 18:00]
# Tool: Cowork (automated session end)
# Written by: Claude scheduled task at 18:00

---

## WHAT WE DID TODAY

No work committed today. No commits found between midnight and 18:00 on 2026-07-18.

---

## PENDING SAEED APPROVALS (from PROJECT_MEMORY)

1. The 3 security items at the top of PROJECT_MEMORY.md — unauthenticated intake endpoint, HMAC secret in git history (needs rotation), directory ACLs on C:\JeffLocal + config. Fix script for item 3 is ready: scripts\service_control\fix_directory_acl.ps1. Saeed to run manually (admin PowerShell). Carry forward every session until done.
2. Multi-tenancy — in build, steps 1-3 of 8 done and deployed 2026-07-17. Separate database per tenant. Step 4 (stand up stmarks instance) next, still blocked by open item #3 (config ACL).
3. Real staff accounts — provide names, roles, emails to unblock pilot go-live.
4. Governance gates 1-7 sign-off — cannot be delegated.
5. JEFF_WEBHOOK_SECRET — set before any live Jeff traffic (endpoint is open until then).
6. n8n API key rotation — confirmed "later".
7. St Marks privacy-policy line — drafted on branch draft/privacy-avamed-processor in SMCPHARMA, NOT pushed. Needs pharmacist/DPO review.

---

## OPEN TASKS (from PROJECT_MEMORY)

1. Fix n8n.py:319 HMAC fail-open; remove /api/n8n/test-intake-batch — Security agent, SAEED SIGN-OFF (auth logic). Endpoint is OPEN right now.
2. Rotate voice_agent_hmac_secret.txt (in git history) — Security agent, SAEED. Rotation, not untracking.
3. ACLs on C:\JeffLocal + config (Authenticated Users write) — DevOps agent, SAEED. Both dirs, all ACEs. Also blocks tenant onboarding — verified 2026-07-17 the tenant-config ACL check correctly refuses to start any tenant while config/ is writable like this.

---

## WHAT TO DO TOMORROW

1. Get Saeed to run scripts\service_control\fix_directory_acl.ps1 (admin PowerShell) — this unblocks item #3 above and tenant onboarding step 4 (St Marks).
2. Get Saeed's sign-off on the n8n.py:319 HMAC fail-open fix (item #1) — endpoint is open right now, this is the top security item.
3. Get Saeed's sign-off on rotating voice_agent_hmac_secret.txt (item #2).

---

## GIT STATE

Latest commit: 39ea3d8267b3456f6de4532b44bcaaaf1dcc1829 — fix: replace 4th-grade brief tone with non-technical business/PM tone
Branch: main
