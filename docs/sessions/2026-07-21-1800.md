# SESSION SUMMARY — [2026-07-21 18:00]
# Tool: Cowork (automated session end)
# Written by: Claude scheduled task at 18:00

---

## WHAT WE DID TODAY

- Confirmed multi-tenancy step 4 (tenant2 on port 8766) is fully verified complete and being managed by the watchdog. Logged that step 5 is next.
- Wrote down the naming rule for tenants: each tenant gets a stable short code (tenant1, tenant2...) plus its real display name shown to users.
- Fixed a permissions problem on the config\tenants folder that was stopping tenant2 from starting, and added a script scoped to fix just that folder safely.
- Recorded the decision on how staff log in: a "Staff Login" link, with each tenant's login page branded to that tenant.
- Found that an old, orphaned watchdog process was running stale code and blocking tenant2. Hardened the tenant2 setup script to force-kill orphaned watchdogs before restarting.
- Fixed a syntax bug in the scheduled-task registration script (a PowerShell switch parameter was written wrong) and discovered along the way that the GDPR weekly purge task had never actually been registered — flagged as a compliance finding.
- Closed out the session with memory/log entries confirming step 4's full verification and queuing step 5.

---

## PENDING SAEED APPROVALS (from PROJECT_MEMORY)

1. The 3 security items at the top of PROJECT_MEMORY.md — unauthenticated intake endpoint, HMAC secret in git history (needs rotation), directory ACLs on C:\JeffLocal + config. Fix script for item 3 ready: `scripts\service_control\fix_directory_acl.ps1`.
2. Multi-tenancy — steps 1-4 of 8 done and deployed. Step 5 (tenant picker + roles) next.
3. Real staff accounts — need names, roles, emails to unblock pilot go-live.
4. Governance gates 1-7 sign-off — cannot be delegated.
5. JEFF_WEBHOOK_SECRET — must be set before any live Jeff traffic (endpoint is open until then).
6. n8n API key rotation — confirmed "later".
7. St Marks privacy-policy line — drafted on branch, needs pharmacist/DPO review.

---

## OPEN TASKS (from PROJECT_MEMORY)

1. Fix n8n.py:319 HMAC fail-open; remove /api/n8n/test-intake-batch — SAEED SIGN-OFF (auth logic). Endpoint is OPEN right now.
2. Rotate voice_agent_hmac_secret.txt (in git history) — SAEED. Rotation, not untracking.
3. ACLs on C:\JeffLocal + config (Authenticated Users write) — DONE 2026-07-20, Saeed ran the fix script, unblocks tenant onboarding step 4.

---

## WHAT TO DO TOMORROW

1. Move on multi-tenancy step 5 (tenant picker + roles) — rename churchtown to slug tenant1 (display "Churchtown Medical Centre") and repoint port 8765.
2. Confirm the fixed GDPR weekly purge scheduled task actually landed for both churchtown and tenant2 (`Get-ScheduledTask -TaskPath \JeffLocal\` should show both purge tasks).
3. Keep the 3 outstanding security items in front of Saeed — unauthenticated intake endpoint, HMAC secret rotation, and the remaining ACL work — none of these are closed yet.

---

## GIT STATE

Latest commit: f4b73d80d4dc0c0c1b2891f9ea6ba956647beb7d memory: session close — step 4 verified complete, step 5 queued
Branch: main
