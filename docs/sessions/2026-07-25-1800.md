# SESSION SUMMARY — [2026-07-25 18:00]
# Tool: Cowork (automated session end)
# Written by: Claude scheduled task at 18:00

---

## WHAT WE DID TODAY

No work committed today.

---

## PENDING SAEED APPROVALS (from PROJECT_MEMORY)

1. The 3 security items at the top of PROJECT_MEMORY.md — unauthenticated intake endpoint, HMAC secret in git history (needs rotation), directory ACLs on C:\JeffLocal + config. Reminder: fix script for item 3 is ready — `scripts\service_control\fix_directory_acl.ps1`. Saeed to run manually (admin PowerShell). Carry forward until done.
2. Multi-tenancy — steps 1-4 of 8 done and deployed. Next is step 5 (tenant picker + roles).
3. Real staff accounts — provide names, roles, emails to unblock pilot go-live.
4. Governance gates 1-7 sign-off — cannot be delegated.
5. JEFF_WEBHOOK_SECRET — set before any live Jeff traffic (endpoint is open until then).
6. n8n API key rotation — confirmed "later".
7. St Marks privacy-policy line — drafted on branch `draft/privacy-avamed-processor` in SMCPHARMA, not pushed. Needs pharmacist/DPO review.

---

## OPEN TASKS (from PROJECT_MEMORY)

1. Fix n8n.py:319 HMAC fail-open; remove /api/n8n/test-intake-batch — Security — SAEED SIGN-OFF (auth logic). Endpoint is OPEN right now.
2. Rotate voice_agent_hmac_secret.txt (in git history) — Security — SAEED. Rotation, not untracking.
3. Multi-tenancy: separate DB per tenant — Backend — steps 1-4 done + verified. Step 5 (tenant picker + roles) next.

---

## WHAT TO DO TOMORROW

1. Chase Saeed for sign-off on the 3 outstanding security items (unauthenticated intake endpoint, HMAC secret rotation, directory ACLs) — these gate go-live.
2. Push forward multi-tenancy step 5 (tenant picker + roles) once Saeed has bandwidth.
3. Chase real staff account details from Saeed — this is blocking Pilot 1 go-live.

---

## GIT STATE

Latest commit: 9eb1ec6baaada785ed9d99921fccc7de8a22c89c
Branch: main
