MORNING BRIEF - 2026-07-28 - 07:00

---

WHAT WE DID YESTERDAY
- The fifth step of multi-tenancy was deployed to the main system, adding an administrator role that allows support staff to view a single tenant's data.
- Support staff can now view data for one tenant at a time using a new selection page.
- A combined view across multiple tenants was created only through a specific script and cannot be assigned via the staff web interface.
- The staff web interface was tested to ensure it successfully prevents privilege escalation.
- This change involved updating the database structure to allow the role field on staff invitations to accommodate new permissions.
- The system successfully applied this new role to existing tenant databases after a critical data corruption bug was identified and fixed during the migration process.
- This necessary fix for the data error was completed before the update was deployed.
- In the same update, the staff editing function was modified so it no longer automatically reduces an administrator account's privileges.

---

WHAT WE ARE DOING TODAY
- A security fix is required by removing a specific API endpoint due to an authentication failure in the script.
- We are awaiting Saeed's approval for the authentication logic because the endpoint is currently open.
- Saeed needs to rotate the voice agent secret file within the Git history for security purposes.
- The required action is to rotate the secret, not to delete or untrack it.
- The code separating databases per tenant has been merged today, and we are ready for the cutover.

---

WHAT'S STUCK
- Nothing stuck right now.

---

THINGS I NEED YOU TO OK
- [ ] The three security items involve an unauthenticated intake endpoint where the secret key has not been set.
- [ ] The HMAC secret was committed to the code history and must be rotated, not just untracked.
- [ ] Access control lists for the local directory and configuration files need fixing using a provided script.
- [ ] The multi-tenancy documentation still incorrectly states that steps one through three of eight have been completed.
- [ ] The status is outdated because step five of the code has already been merged.
- [ ] Saeed must run the cutover script today before the system switches over.
- [ ] This action requires running the script in an administrator window before tenant1 switches to the live system.
- [ ] Although the code for step five was merged, it has not yet been verified as executed or tested live.
- [ ] We must treat the execution of step five as pending until Saeed confirms that it has been successfully run and tested.
- [ ] We need real staff names, roles, and email addresses to unblock the pilot launch.
- [ ] Sign-off for governance gates one through seven cannot be delegated to others.
- [ ] The JEFF_WEBHOOK_SECRET must be set before any live traffic is allowed.
- [ ] The rotation of the n8n API key has been confirmed for a later time.
- [ ] The draft privacy policy line is located on the branch named draft/privacy-avamed-processor.
- [ ] The SMCPHARMA document has not been pushed and requires review by the pharmacist and Data Protection Officer.

---

Behind the scenes: 4 code change(s) saved today. Ask me any time if you want the details.

Want more detail on anything above? Just ask me next time we talk.
