# TEST FIXTURE ONLY — proves a new tenant can be onboarded by adding one file,
# no code change. Not a real practice. Safe to commit: no patient data, no
# secrets, just plumbing (see _template.env.example for the format).

JEFFLOCAL_TENANT_NAME=Test Tenant 1
JEFFLOCAL_DB_PATH=C:\JeffLocal\dashboard\data\tenants\test-tenant-1.sqlite
JEFFLOCAL_PORT=8770
